FAil2Ban

sudo zgrep 'Ban' /var/log/fail2ban.log*
= show the bans in fail2ban

cat /var/log/fail2ban.log
= show all logs of fail2ban

Nala
sudo nala *
= use nala instead of apt

Linux
rm -r <foldername>
remove all files in folder and delete folder


